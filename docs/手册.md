# mdm-product · 使用手册

## 1. 项目结构

```
mdm-product/
├── component.yaml            # 平台读的那份
├── assembly.yaml             # 只有 be-ops 读
├── contracts/
│   ├── product.proto         # gRPC 契约
│   ├── product.openapi.yaml  # HTTP 契约（对外 REST）
│   └── events/
│       └── product.events.json
├── backend/
│   ├── module/module.go      # 唯一入口 New(ctx, rt)
│   ├── cmd/server/main.go    # 一行 besdk.RunStandalone(module.New)
│   └── internal/...
├── migrations/
│   ├── 001_xxx.up.sql
│   └── 001_xxx.down.sql
├── Dockerfile
├── Makefile
└── README.md
```

（详见总纲 §4 SOP-B 的标准布局）

## 2. 功能详解

（随 Task 6 实现同步补）

## 3. 完整契约清单

（随 Task 5 契约同步补）

## 4. 数据模型

（随 Task 5 迁移同步补）

## 5. 开发

（随 Task 6 实现同步补）

## 6. 排障

（随实现过程中遇到的真实问题同步补，参照根 `docs/dev/实测踩坑记录.md` 的四栏格式）
